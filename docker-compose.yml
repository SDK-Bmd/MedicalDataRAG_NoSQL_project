services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_mtsamples
    ports:
      - "6333:6333"   # REST API + dashboard
      - "6334:6334"   # gRPC
    volumes:
      - ./qdrant_storage:/qdrant/storage
    restart: unless-stopped